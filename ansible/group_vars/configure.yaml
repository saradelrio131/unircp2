---
# NFS Shared conf
nfs_dir: /srv/nfs

# k8s master node conf
master: 10.0.1.10
master_hostname: kubemaster
master_fqdn: kubemaster.acme.es

# k8s worker node conf
worker: 10.0.1.11
worker_hostname: kubenode
worker_fqdn: kubenode.acme.es

# Copy here the command generated by kubeadm init (output of 04-playbook.yaml)
# Format: kubeadm join --token <token> <master-ip>:<master-port> --discovery-token-ca-cert-hash sha256:<hash>
token: iwkbce.p6ch4ts47x8weeni
masterip: 10.0.1.10
masterport: 6443
hash: 9ee95c7923fec6994d8d22582fd02457cdef2de9cebfbee08151f2fe1a0ef0eb

# Non-root user to manage the k8s cluster
uusername: kubeadmin
upassword: changeme
